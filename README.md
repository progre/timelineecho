## Build

### on Windows

```powershell
cargo lambda build --release --compiler cross
```

### on macOS

```bash
cargo lambda build --release
```

## Deploy

```bash
cargo lambda deploy --profile ???
```
